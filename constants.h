#ifndef constants_h
#define constants_h

#include <map>

using namespace std;

namespace constants {
    enum NodeTypes {
        PLUS, MINUS, NUMBER, VARIABLE, POWER, TIMES, DIVISION, SIN, COS, PI, ROOT
    };
}
#endif